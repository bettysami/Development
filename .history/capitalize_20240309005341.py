# ask user their name
name = input("what's your name? ")

# Remove Whitespace from str
name = name.strip()

# Capitalize user's name
name = name.capitalize()

# Say hello to user
print(f"hello, {name}")

#But the abode capitalizes only the first word