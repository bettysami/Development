# >  means greater than
# >= means greater than or equal to
# < means less than
# <= means less than or equal to
# == in python means this compares the thing on the left and right
# != means not equal to






x = int(input("what's x? "))
y = int(input("What's y? "))

# let's change the values of these variables
if x < y:
    print("x is less than y")

if x > y:
    print("x is greater than y")    

if x == y:
    print("x is equal to y")

# Well, this if looks repetitive, let's introduce elif function, go to elif.py

#let's introduce boolean expression
        